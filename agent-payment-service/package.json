{
  "name": "payme-agent-payment-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch index.js",
    "start": "node --env-file=.env index.js",
    "restart": "lsof -ti :3001 | xargs kill -9 2>/dev/null; sleep 1; node --env-file=.env index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.15.0",
    "express": "^5.1.0"
  }
}
