# Agent 2 (Payee) – receives payment requests, returns PayMe links
PORT=3002
# PayMe agent payment service (create-link). Use 127.0.0.1 so fetch works; agent-payment-service must be running on 3001.
AGENT_SERVICE_URL=http://127.0.0.1:3001
# Optional: base URL for payment_link in responses (e.g. https://your-payme.com)
# PAYMENT_LINK_BASE_URL=https://your-payme.com
